/**
 * Command Database for Sight
 *
 * Re-exports the canonical CommandDatabase from command-database module.
 * This module exists for backward compatibility with existing imports.
 */

export { CommandDatabase, command_database } from '../command-database';
