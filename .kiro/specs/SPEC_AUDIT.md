# Sight LSP Specification Audit

**Date:** 2026-01-01  
**Total Specs Audited:** 88

| Spec Name | Status | Notes |
|-----------|--------|-------|
| by-varlist-parsing | Partially Implemented | Parser recognizes 'by' prefix and has AST structure (PrefixNode with varlist field), but varlist parsing is not implemented (TODO comment in parser). No bysort/sort modifier support. |
| c-local-cross-file-support | ✅ Implemented | Analyzer accepts workspace symbols, extracts c_locals from programs, registers them at call sites, and diagnostics provider handles cross-file c_local suppression |
| called-from-directive | ✅ Implemented | Working directory directive fully implemented with all synonyms (@lsp-working-directory, @lsp-working-dir, @lsp-current-directory, @lsp-current-dir, @lsp-cd, @lsp-wd), path resolution logic in forward scope resolver, fallback strategies, inheritance from parent files, comprehensive test coverage, and complete README documentation |
| callee-revalidation-on-caller-change | Implemented | Full implementation found: reverse dependency tracking with CallEdge/InterfaceHash types, update_reverse_dependencies method, schedule_callee_revalidation with prioritization, cascade invalidation, and integration tests |
| close-brace-diagnostic | ✅ Implemented | All 6 requirements implemented: validate_close_brace() and validate_open_brace() methods detect code before/after braces, else on same line as close brace, open brace alone, and code after open brace. Error codes 3001/3002 defined and used. Property tests verify functionality. |
| command-database-cleanup | ✅ Implemented | Dead CommandMetadata, CommandSyntax, CommandOption, StoredResult, CrossReference, CommandCache, and AbbreviationDict interfaces removed from types/index.ts. StataVersion type retained. TypeScript compiles without errors. |
| command-database-integration | ✅ Complete | Cache has 1686 commands (vs 148 legacy), providers use new database, TypeScript compiles, superset validation passes |
| command-metadata-system | Superseded | Requirements superseded by smcl-syntax-cleanup. Implementation exists: CommandDatabase class, SMCL extractor, cache generation script, JSON cache files, comprehensive tests. All original requirements met but spec marked as superseded due to incompatible changes. |
| command-name-expansion | Not Implemented | Command database has abbreviation expansion functionality (`expand_abbreviation`, `lookup` with abbreviation support) but parser has TODO comments indicating expansion is not implemented. Parser stores original text but does not resolve to canonical forms. |
| comment-style-normalization | Partially Implemented | Core normalization logic exists in CommentProcessor with config validation, but missing format-on-save integration and some advanced features like comment line width wrapping |
| completion-improvements | Superseded | Requirements superseded by completion-improvements-fixes. Trigger chars include space/comma (violates Req 6), descriptions removed from cache (meets Req 2), priority tiers implemented (meets Req 5) |
| completion-improvements-fixes | Implemented | All 4 requirements are implemented: (1) includeAbbreviations setting not found in codebase - already removed/never existed, (2) newline trigger character defensive early return present at line 1050, (3) fallback completions check for empty prefix and return empty list at line 1885, (4) cache keys normalized to lowercase throughout command database |
| comprehensive-property-tests | ✅ Implemented | All 8 requirements fully implemented with 100+ property tests covering parser round-trip, lexer tokenization, completion relevance, diagnostic accuracy, formatting preservation, go-to-definition correctness, hover completeness, and symbol information completeness. Tests use fast-check with 100 iterations each and proper shrinking. |
| confirm-variable-registration | ✅ Implemented | Full implementation found: analyzer extracts variables from `confirm variable`/`confirm var` commands, supports prefixes (capture, quietly), handles options, VariableSymbol includes 'confirm' source type, property-based tests exist |
| cross-file-awareness | Superseded | Spec marked as superseded by done-by-locals-bug due to inheritance rule changes. Implementation exists with directive parser, scope resolver, workspace indexer, 128+ test files, but inheritance semantics changed to exclude locals from @lsp-done-by (only @lsp-included-by inherits locals). Core features implemented: symbol parsing, directive parsing, scope resolution, workspace indexing, autocompletion, go-to-definition, diagnostics, configuration support. |
| cross-file-awareness-fixes | ✅ Implemented | All requirements met: directive presence detection uses parsed directives (scope-resolver:574), workspace config uses proper types (DeepPartial<StataLSPConfig>), scope resolver uses logger interface with fallback (scope-resolver:181-200). Property tests verify behavior. |
| cross-file-forward-reference-fix | ✅ Implemented | Forward reference detection preserved for same-file symbols via sourceUri comparison (lines 817, 823, 831, 836, 841 in diagnostics.ts). Cross-file suppression working. Token-level macro detection in strings implemented (check_token_macro_references method). All requirements met. |
| current-file-forward-calls | ✅ Implemented | Forward calls from current file are resolved via ForwardScopeResolver and integrated into ResolvedScope.forward_call_symbols. Providers use position-aware visibility filtering. |
| diagnostic-false-positives | ✅ Implemented | Inline mata/python context handled (lexer), args command macro definitions implemented (analyzer), macro path detection and skipping implemented (analyzer/forward-scope-resolver) |
| directive-call-site-diagnostics | Implemented | All 7 requirements implemented in ScopeResolver with comprehensive diagnostics for call site identification failures, directive/call-type mismatches, mixed call types, explicit parameter validation, and configurable severity levels. Property-based tests verify implementation. |
| directive-syntax-flexibility | ✅ Implemented | All 6 requirements fully implemented: optional colon/quotes syntax, .do extension fallback, backward compatibility, comment style support. Comprehensive test coverage in unit and property tests. |
| do-command-arguments | ✅ Implemented | File path extraction correctly handles quoted/unquoted paths and separates from script arguments in `detect_forward_call()` method |
| document-symbols-enhancement | ✅ Implemented | All requirements implemented: scalars/matrices included with SymbolKind.Variable and correct details, local macros hierarchically nested under programs with range containment logic, comprehensive test coverage in symbols.test.ts |
| done-by-locals-bug | ✅ Implemented | Local macro inheritance correctly restricted for @lsp-done-by directives. apply_inheritance_rules() excludes locals for 'done-by' type, includes all symbols for 'included-by' type. Tests pass for both unit and integration scenarios. |
| egen-command-parsing | Partial | Basic variable extraction implemented in analyzer, but missing specialized function parsing, completion support, and complex syntax handling |
| else-block-symbol-registration | ✅ Implemented | Analyzer handles else blocks via process_control_flow method (line 471), property tests validate macro registration in else blocks |
| embedded-language-detection | ✅ Fully Implemented | Complete implementation with ContextTracker class supporting Mata/Python block detection, context switching, diagnostics suppression, completion filtering, hover filtering, symbol navigation, formatting preservation, and robust error handling. Comprehensive test coverage with 11 property tests and 30+ integration tests. |
| expression-keyword-disambiguation | ✅ Implemented | Parser correctly handles keywords as variables in expressions via parseIfQualifierExpression(), comprehensive test coverage (13/13 passing), supports complex cases with operators and parentheses |
| extended-macro-functions | Implemented | Parser supports colon syntax, analyzer recognizes definitions, completion provider handles context detection, test coverage exists |
| file-path-handling | Implemented | Parser has parseFilePathArgument() method that coalesces unquoted path tokens for file commands. Completion provider detects directive and command path contexts, provides file path completions for @lsp-* directives and file commands (do/run/include). File path utilities define FILE_COMMANDS and PATH_DIRECTIVES sets. |
| fix-property-test-failures | ✅ Implemented | Comprehensive test suites added for syntax option parsing edge cases and macro detection with incomplete syntax. All 2708 tests passing including property-based tests. |
| forward-macro-reference-detection | ✅ Implemented | Forward reference detection using preorder_index, positional args bypass, @lsp-ignore-next applied before position checks, first definition wins for multiple definitions, preorder_index reset on analyze() |
| forward-scope-resolution | ✅ Implemented | Full implementation with ForwardScopeResolver class, directive parsing (@lsp-do, @lsp-run, @lsp-include), automatic command detection, scope inheritance rules, caching, call-site filtering, recursive resolution, and comprehensive test coverage |
| forward-scope-working-directory-fix | Implemented | Two-pass resolution with discover_working_directory(), cache keys include working directory, forward calls use effective working directory from directive chain |
| frame-prefix-command | ✅ Implemented | Frame command exists in builtin-commands.ts with subcommands. Bogus 'framework' command removed from cache and SMCL extractor (added to NON_COMMAND_TOKENS). All framework abbreviations (fra, fram, frame, framew, framewo, framewor) removed. Test added to verify exclusion. |
| gettoken-macro-creation | ✅ Implemented | Full implementation with comprehensive test coverage. Analyzer extracts macros from both `gettoken` and `gettok` commands, supports single/dual output macros, handles options, respects scoping rules, and includes forward reference detection. All 18 test cases pass. |
| global-macro-execution-order | ✅ Implemented | Line indexing is 0-indexed throughout, diagnostic formats are consistent, call site inference exists via find_match_line, execution order checking implemented. README updated with Execution Order Checking section documenting within-file analysis, cross-file call sites, out-of-scope warnings, and automatic call site detection. |
| hover-multi-symbol-display | ✅ Implemented | Multi-symbol display with SymbolMatch interface, collect_all_symbol_matches(), format_multi_symbol_hover(), variable hover enhancements with format_variable_hover(), cross-file resolution support, comprehensive property tests |
| hover-out-of-scope-display | ✅ Implemented | Full implementation with comprehensive testing. HoverProvider includes get_out_of_scope_hover(), get_reference_type_from_context(), and collect_all_symbol_matches() methods that handle all requirements. Integration tests, property tests, and unit tests verify correct behavior for local/global macro syntax detection, "(out of scope)" indicator display, source information inclusion, and prevention of fallthrough to other symbol types. |
| large-file-indexing-policy | ✅ Implemented | Configurable threshold (500KB default), skips large files during indexing, logs debug messages, tracks metrics, indexes opened large files regardless of size |
| local-global-macro-completion | ✅ IMPLEMENTED | All requirements fully implemented and tested. Completion provider correctly filters local macros for backtick prefix (`), global macros for dollar prefix ($), with proper labeling and scope handling. Comprehensive property-based tests (9 tests) and unit tests (55 tests) all passing. |
| logging-refactor | ✅ Implemented | Logger service created with singleton pattern, verbosity control, LSP channel routing. All production files converted from console.* to logger.*. Server initializes Logger with connection.console.log. Scripts preserve console.* calls. |
| lsp-declare-macros | ✅ Implemented | Full implementation found: directive parser recognizes @lsp-local/@lsp-global patterns, analyzer processes declarations with forward-only scoping, symbols added to completion/diagnostics, comprehensive test coverage including regression tests |
| lsp-declare-symbols | ✅ Implemented | All 5 declaration directives (@lsp-local, @lsp-global, @lsp-scalar, @lsp-matrix, @lsp-program) are implemented in directive parser, registered in analyzer, suppress undefined warnings with forward-only effect, validate single argument constraint, and are documented in README |
| lsp-performance-optimization | ✅ Implemented | Debouncing, Line offset index, Async workspace indexing, Binary search context lookup, Prefix caching all implemented. Unified context tracker initialization fixed - Parser now accepts optional context_tracker parameter and reuses DocumentStore's instance. Redundant parsing elimination verified - Document_Store parses once per version, Diagnostics_Provider reuses cached results. |
| macro-case-sensitivity | ✅ IMPLEMENTED | Prefix filtering implemented in get_macro_completions() with case-insensitive matching (lines 1584-1587), prefix extraction from replacement range (lines 1528-1537), and proper handling of empty prefix returning all macros |
| macro-completion-with-closing-quote | Implemented | Full implementation with macro context detection, replacement range computation, closing delimiter detection, and comprehensive property-based tests covering all requirements |
| macro-creating-options | ✅ Implemented | Full implementation with hardcoded allowlist (levelsof, glevelsof), option parsing with identifier validation, user-defined program detection via c_local/global patterns, comprehensive property-based tests, and integration in main analyzer |
| macro-creating-options-first-definition | ✅ Implemented | First definition semantics correctly implemented in analyzer with "first definition wins" logic, tracks additional definitions in `additional_definitions` array, but missing duplicate option warnings |
| macro-test-scenarios | ✅ Implemented | All required test scenarios exist: completion tests for `local apple sauce` with `a` and `A` prefixes, multiple macro suggestions with `ap` prefix, and diagnostic tests for case-sensitive undefined macro warnings including exact macro name in messages |
| mata-brace-blocks-context-tracker | ✅ Implemented | Full implementation in ContextTracker with brace depth tracking, LBRACE/RBRACE handling, and comprehensive property-based tests (6/6 passing) |
| namelist-argument-type | ✅ Implemented | Type definition includes 'namelist', parser recognizes it, analyzer registers implicit locals, comprehensive property tests verify functionality |
| option-completion-after-comma | ✅ Implemented | Option completion after comma is fully implemented with `detect_option_context()` function that detects cursor position after commas, extracts command names (handling prefixes and colon syntax like "merge 1:m"), and `get_option_completions()` provides filtered options. Hover provider has `is_in_option_context()` method that correctly identifies option context and suppresses command hover behavior. |
| option-completion-trigger | ✅ Implemented | Comma and space are trigger characters (line 103), option context detection works (lines 327-384), empty prefix returns all options for built-in commands (lines 1411-1433) and user programs (lines 2684-2690) |
| option-extraction | Superseded | Requirements superseded by smcl-syntax-cleanup. Implementation exists: SMCL extractor includes comprehensive option parsing (ExtractedOption interface, parse_option_pattern function), command cache includes options field, completion provider uses options for completions. Status marked as superseded in requirements. |
| option-name-expansion | Not Implemented | Parser has TODO comments for option abbreviation expansion but no implementation. OptionNode.fullName always equals name. No option dictionary or expansion logic found. |
| optional-do-extension | ✅ Implemented | Full implementation found: `resolvePathWithDoFallback()` in file-path-utils, integrated across ForwardScopeResolver, ScopeResolver, DirectiveParser, and DefinitionProvider. Comprehensive property tests verify all 7 requirements including fallback resolution, exact path precedence, diagnostics, and cross-file scope resolution. |
| orphan-end-diagnostic | ✅ Implemented | Context tracker detects orphan `end` statements via `validate_end_delimiters()`, emits `UNEXPECTED_END` diagnostics with message "Unexpected 'end' command - not closing any program, mata, or python block", includes program block tracking to avoid false positives |
| out-of-scope-diagnostic-message | ✅ Implemented | All requirements implemented: correct diagnostic messages for inheritance-excluded locals, deduplication with priority, accurate call site lines, and hover provider suppression for unrelated symbol types |
| parent-forward-call-diagnostics | ✅ Implemented | Forward call diagnostic remapping in scope resolver, diagnostic filtering in diagnostics provider, comprehensive test coverage |
| parent-forward-call-inheritance | ✅ Implemented | Full implementation found in ScopeResolver.resolve_parent_forward_calls() with comprehensive test coverage including integration and property tests |
| parenthesis-in-completion-prefix | ✅ Implemented | textEdit and filterText implemented for all completion types: macro, variable, scalar, and matrix completions. Replacement range computed from word prefix to cursor position. Completions after '(' no longer include the parenthesis. |
| prefix-colon-and-program-context | ✅ Implemented | Parser supports prefix commands with colons (lines 662-665), context-aware program keyword detection (line 114), and statement keywords as identifiers in command arguments. Tests pass for all requirements. |
| python-block-end-fix | ✅ Implemented | Lexer correctly recognizes `end` as END_PYTHON token, parser handles END_PYTHON, context tracker validates and warns against `end python`, completion suggests only `end` |
| quote-auto-delete-simplification | Implemented | Core logic in `compute_deletion_cleanup()` with simplified rules: backtick+apostrophe→delete, quote+quote→delete, apostrophe→no-op. Full VS Code integration with document change handlers, line caching for deletion detection, and comprehensive test coverage including unit tests and property-based tests. |
| quote-snippets | Implemented | Full implementation found with comprehensive auto-closing pairs for Stata quote patterns. VS Code language configuration includes basic ` → `' auto-closing. Advanced nested patterns (compound strings, nested macros) handled by custom TypeScript implementation in client/src/quote-auto-close-core.ts with skip-over behavior, deletion cleanup, and extensive test coverage. All 10 requirements appear to be implemented. |
| quoted-path-parsing | ✅ IMPLEMENTED | Parser already handles STRING tokens in command varlist (lines 766, 784, 934, 1062 in parser/index.ts). Analyzer correctly processes quoted paths in detect_forward_call method (lines 950-957 for complete quotes, 958-979 for partial quotes with macros). Tests show quoted paths working in forward call resolution. |
| rename-command-options | ✅ Implemented | All 7 options (addnumber, renumber, sort, dryrun, upper, lower, proper) implemented in builtin-commands.ts with correct argument flags and abbreviations. Comprehensive integration tests validate all requirements. |
| rename-to-sight | ✅ Implemented | Package names updated to "sight-language-server" and "sight-client", VS Code extension display name is "Sight - Stata Language Server", configuration uses "sight." prefix, commands use "sight." prefix, workspace config uses ".sight.json", repository URL updated to "jbearak/sight" in client/package.json. |
| rename-variable-registration | ✅ Implemented | Full implementation found: extract_rename_variables() handles simple/grouped syntax, supports 'ren' abbreviation, registers variables with source='rename', includes wildcard/pattern detection to skip non-static renames, VariableSymbol type includes 'rename' in source union |
| restore-test-regime | ✅ Implemented | Parser error handling tests exist with "Missing" and "Missing closing brace" messages. Document symbol tests include embedded blocks (mata/python). Test infrastructure runs without errors. DEFAULT_CONFIG uses proper literal types for severity values. |
| scope-cache-optimization | Implemented | Separate cache invalidation paths, content hash validation, nested metrics with backward compatibility, and proper cascade behavior all implemented |
| smcl-command-extraction | Superseded | Requirements superseded by smcl-syntax-cleanup. Implementation exists in src/command-database/smcl-extractor.ts with comprehensive SMCL parsing including viewerdialog, cmdab patterns, multi-command extraction, and option parsing, but spec marked as superseded due to incompatible changes. |
| smcl-syntax-cleanup | ✅ Implemented | Syntax field completely removed from CommandInfo interface in types.ts, all syntax field assignments removed from addon-commands.ts, and conditional syntax assignments removed from command-database/index.ts. TypeScript compiles without errors. |
| stata-lsp | ✅ Implemented | Full LSP implementation with lexer, parser, semantic analyzer, completion, diagnostics, hover, go-to-definition, symbols, formatting, workspace indexing, and VS Code client extension. All 13 requirements met with comprehensive feature set including cross-file resolution, embedded language support, and configurable diagnostics. |
| symbol-source-file-links | ✅ Implemented | Hover provider includes `format_source_link()` method that generates clickable markdown links `[display_path](file_uri)` for cross-file symbols. Returns empty string for same-file symbols. Comprehensive test coverage includes link formatting, path escaping, workspace-relative paths, and non-file URI handling. |
| syntax-command-bugs | ✅ IMPLEMENTED | Both requirements fully implemented: 1) Parser correctly handles prefixed syntax commands (qui syntax, cap syntax) as SyntaxNodes with proper prefix attachment and signature extraction; 2) All weight argument types (weight, fweight/fw, aweight/aw, pweight/pw, iweight/iw) recognized and create implicit 'weight' and 'exp' local macros. Comprehensive test coverage with 100% pass rate. |
| syntax-command-parsing | ✅ Implemented | Parser creates SyntaxNode AST, analyzer registers implicit locals, completion/hover providers support user program signatures |
| syntax-command-simplification | Implemented | Test passes - syntax commands emit zero diagnostics as required |
| test-failure-fixes | Implemented | All 5 requirements implemented: context tracker error codes (MISMATCHED_END_PYTHON), parser content extraction, extended macro recognition, symbol provider embedded blocks, and unab AST handling. All tests passing (2708/2708). |
| testable-server-refactor | ✅ Implemented | Handler logic extracted to server-handlers.ts with factory functions, dependency injection via HandlerDependencies interface, LSP lifecycle tests exist in tests/integration/lsp-lifecycle.test.ts, backward compatibility maintained via server-factory.ts |
| token-macro-forward-reference | Implemented | Token-only macro references are checked via check_token_macro_references() method using line-based position comparison, consistent with AST references |
| unify-forward-call-feeds | ✅ Implemented | Handler skips forward_scope computation when scope_resolver available (L454-455 server-factory.ts), diagnostics provider uses resolved_scope.forward_call_symbols as primary source with forward_scope fallback (L275-290 diagnostics.ts), comprehensive test coverage exists |
| user-configurable-settings | ✅ FULLY IMPLEMENTED | All LSP settings exposed via VS Code configuration with proper types, defaults, and descriptions. Configuration changes propagate correctly to server. README documents all settings comprehensively. |
| working-directory-chain-inheritance | ✅ Implemented | Working directory inheritance through directive chains is fully implemented in ScopeResolver with discover_working_directory() method, propagation to ForwardScopeResolver via context, and diagnostic source attribution with file/line mapping |
| working-directory-inheritance | ✅ Implemented | @lsp-run-by synonym implemented in directive parser, working directory inheritance implemented in scope resolver with proper precedence rules, comprehensive test coverage, documented in README |
| working-directory-propagation | ✅ Implemented | Working directory propagation via `callee_result.working_directory ?? my_context.working_directory` (line 300), diagnostic call chain tracking via `call_chain` array, and path resolution with working directory context in `resolve_call_path()` method |
| workspace-root-fallback-fix | ✅ Implemented | Server sets workspace root on document store during initialization and workspace changes. Analyzer implements workspace root fallback logic in resolve_forward_call_path method. Document store passes workspace_root to analyzer config. |

---
*Generated by automated spec audit*
