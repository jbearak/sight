{
  "name": "sight-language-server",
  "version": "0.4.2",
  "type": "module",
  "workspaces": [
    "client"
  ],
  "description": "Language Server Protocol implementation for Stata",
  "main": "dist/cli.js",
  "bin": {
    "sight-language-server": "dist/sight-server.js"
  },
  "scripts": {
    "build": "tsc && cp -r src/command-database/caches dist/command-database/ && cd client && npm run compile && npm run copy-server && cd ..",
    "build:bundle": "bun scripts/build-binary.ts bundle",
    "build:binary": "bun scripts/build-binary.ts binary",
    "build:current": "bun scripts/build-binary.ts current",
    "build:all": "bun scripts/build-binary.ts all",
    "build:npm": "bun run build:bundle && npm run validate:npm",
    "validate:npm": "node dist/sight-server.js --version && echo 'NPM build validation passed'",
    "prepublishOnly": "npm run build:npm",
    "publish:npm": "npm publish",
    "publish:dry-run": "npm publish --dry-run",
    "install:binary": "bun scripts/install.ts",
    "uninstall:binary": "bun scripts/uninstall.ts",
    "watch": "tsc --watch",
    "typecheck:scripts": "tsc -p tsconfig.scripts.json --noEmit",
    "typecheck": "tsc --noEmit && bun run typecheck:scripts && cd client && tsc --noEmit",
    "test": "bun run typecheck; bun test ./tests",
    "test:watch": "bun test --watch ./tests",
    "lint": "eslint -c .eslintrc.cjs src/**/*.ts",
    "clean": "rm -rf dist bin",
    "package": "bun run build && npm install --no-package-lock && cd client && bun run copy-server && bun run package",
    "version:patch": "bun scripts/bump-version.ts patch",
    "version:minor": "bun scripts/bump-version.ts minor",
    "version:major": "bun scripts/bump-version.ts major",
    "release": "bun scripts/release.ts"
  },
  "keywords": [
    "stata",
    "lsp",
    "language-server",
    "language-server-protocol",
    "ide",
    "editor",
    "autocomplete",
    "syntax-highlighting",
    "go-to-definition",
    "diagnostics",
    "typescript"
  ],
  "author": "Jonathan Marc Bearak",
  "license": "GPL-3.0",
  "dependencies": {
    "@jbearak/dta-parser": "github:jbearak/dta-parser#v0.1.1",
    "vscode-languageserver": "^9.0.1",
    "vscode-languageserver-textdocument": "^1.0.11",
    "vscode-uri": "^3.0.8"
  },
  "devDependencies": {
    "@glideapps/glide-data-grid": "^6.0.3",
    "@types/jest": "^29.5.8",
    "@types/node": "^20.10.0",
    "@typescript-eslint/eslint-plugin": "^6.13.0",
    "@typescript-eslint/parser": "^6.13.0",
    "bun-types": "^1.3.5",
    "esbuild": "^0.27.2",
    "eslint": "^8.54.0",
    "fast-check": "^3.15.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1",
    "typescript": "^5.3.0",
    "vscode-oniguruma": "^2.0.1",
    "vscode-textmate": "^9.3.0"
  },
  "engines": {
    "node": ">=20.0.0",
    "bun": ">=1.0.0"
  }
}
